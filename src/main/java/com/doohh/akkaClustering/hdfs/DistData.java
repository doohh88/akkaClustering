package com.doohh.akkaClustering.hdfs;

public class DistData {
	/*
	private String hdfsPath = "hdfs://163.152.21.205:9000";
	private Configuration conf;
	FileSystem fs;
	
	public DistData(String hdfsPath) {
		this.hdfsPath = hdfsPath;
		try {
			init();			
		} catch (Exception e) {
			e.printStackTrace();
		}
	}
	
	public void init() throws Exception {
		conf = new Configuration();
		FileSystem fs = FileSystem.get(new URI(this.hdfsPath), conf);
	}	
	
	public void readFile(String path){
		Path filePath = new Path(path);

	}
	*/
}
